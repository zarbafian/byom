# Per-run evidence (regenerated by every mode; the sheet names the paths).
evidence/
# The kovee-side driver's own build products and resolved lockfile: it is
# its own workspace, pinned by kovee's constraints, and nothing in byom's
# lockfile may depend on it.
kovee-driver/target/
kovee-driver/Cargo.lock
__pycache__/
