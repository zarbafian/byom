{
  "name": "envelope/failure-invalid-unknown-kind",
  "description": "problem kind outside the closed §14.9 enum fails closed even with well-formed type and title; new kinds are a spec revision (R0/BYOM-02: required members added so the unknown kind is the isolated defect)",
  "input": {
    "schema": "bpp-failure",
    "value": {
      "outcome": "problem",
      "problem": {
        "type": "https://byom.dev/problems/novel_problem",
        "title": "Novel problem",
        "kind": "novel_problem"
      }
    }
  },
  "expected": {
    "valid": false,
    "reason": "kind not in the closed problem-kind enum"
  }
}
