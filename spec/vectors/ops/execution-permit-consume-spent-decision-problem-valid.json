{
  "name": "ops/execution-permit-consume-spent-decision-problem-valid",
  "description": "double consume with a different key: a different stable_execution_key cannot consume the spent one-shot decision; exact replay of the original canonical request and key returns the identical receipt (§13.1 step 6, gap note G37) (R0/BYOM-02: required problem type added, https://byom.dev/problems/<kind>)",
  "input": {
    "schema": "bpp-failure",
    "value": {
      "outcome": "problem",
      "problem": {
        "kind": "stale_revision",
        "type": "https://byom.dev/problems/stale_revision",
        "status": 409,
        "title": "one-shot execution decision already consumed",
        "detail": "the authorized ActIntent moved to consumed under stable_execution_key exec-key-410; a consume with a different key cannot claim the same one-shot decision or exhausted ceiling — recover the original receipt by replaying the identical canonical request and key"
      }
    }
  },
  "expected": {
    "valid": true
  }
}
