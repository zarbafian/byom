{
  "name": "ops/membership-offer-revoke-request-missing-expected-revision-invalid",
  "description": "RT-01: membership_offer_revoke is update-classed (current-head CAS) — its closed update meta REQUIRES meta.expected_revision, so omitting the last observed revision fails structurally, never as an optional runtime nicety (§14.2 'Updates require the last observed revision').",
  "input": {
    "schema": "membership-offer-revoke-request",
    "value": {
      "version": "0.2",
      "op": "membership_offer_revoke",
      "meta": {
        "request_id": "request-id-1",
        "idempotency_key": "idempotency-key-1",
        "expected_endpoint_incarnation": "expected-endpoint-incarnation-1",
        "expected_recovery_epoch": 1
      },
      "offer_ref": "offer-ref-1",
      "revoked_by_decision_ref": "revoked-by-decision-ref-1"
    }
  },
  "expected": {
    "valid": false
  }
}
