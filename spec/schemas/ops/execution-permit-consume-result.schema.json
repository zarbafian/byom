{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://byom.example/bpp/v0/ops/execution-permit-consume-result.schema.json",
  "title": "BPP execution_permit_consume result (DESIGN.md §13.1, §14.8; registry R34 runtime)",
  "description": "Success.result for execution_permit_consume: the verbatim §13.1 ExecutionConsumptionReceipt with max_uses pinned to 1 (one-shot; gap note G37, G4 invariant-pinning precedent). If the host crashes after consumption it recovers this same receipt rather than asking for new authority; the host intersects it with stricter current platform policy, mints its local permit, and only then creates a driver attempt (§13.1 steps 6–7).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "receipt_id",
    "byom_endpoint_ref",
    "endpoint_incarnation",
    "recovery_epoch",
    "intent_ref",
    "intent_digest",
    "mandate_use_ref",
    "mandate_use_digest",
    "stable_execution_key",
    "subject_digest",
    "driver_audience",
    "participant_ref",
    "budget_reservation_set_ref",
    "issued_at",
    "expires_at",
    "max_uses",
    "digest"
  ],
  "properties": {
    "receipt_id": {
      "$ref": "#/$defs/identifier"
    },
    "byom_endpoint_ref": {
      "$ref": "#/$defs/identifier"
    },
    "endpoint_incarnation": {
      "$ref": "#/$defs/identifier"
    },
    "recovery_epoch": {
      "$ref": "#/$defs/safeNonNegativeInteger"
    },
    "intent_ref": {
      "$ref": "#/$defs/identifier"
    },
    "intent_digest": {
      "$ref": "#/$defs/digestRef"
    },
    "mandate_use_ref": {
      "$ref": "#/$defs/identifier"
    },
    "mandate_use_digest": {
      "$ref": "#/$defs/digestRef"
    },
    "stable_execution_key": {
      "$ref": "#/$defs/identifier"
    },
    "subject_digest": {
      "$ref": "#/$defs/digestRef"
    },
    "disclosure_digest": {
      "$ref": "#/$defs/digestRef"
    },
    "driver_audience": {
      "$ref": "#/$defs/identifier"
    },
    "participant_ref": {
      "$ref": "#/$defs/identifier"
    },
    "episode_ref": {
      "$ref": "#/$defs/identifier"
    },
    "episode_fence_digest": {
      "$ref": "#/$defs/digestRef"
    },
    "budget_reservation_set_ref": {
      "$ref": "#/$defs/identifier"
    },
    "issued_at": {
      "$ref": "#/$defs/timestamp"
    },
    "expires_at": {
      "$ref": "#/$defs/timestamp"
    },
    "max_uses": {
      "const": 1,
      "description": "Invariant constant: an ExecutionConsumptionReceipt is one-shot (§13.1)."
    },
    "digest": {
      "$ref": "#/$defs/digestRef"
    }
  },
  "$defs": {
    "protocolVersion": {
      "type": "string",
      "pattern": "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)$",
      "description": "Negotiated protocol minor version, e.g. \"0.2\" (§14.1)."
    },
    "identifier": {
      "type": "string",
      "pattern": "^[\\x21-\\x7e]{1,128}$",
      "description": "Opaque visible-ASCII identifier, at most 128 bytes (§14.9)."
    },
    "safeNonNegativeInteger": {
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991,
      "description": "I-JSON safe integer (§14.2 rejects unsafe integers)."
    },
    "timestamp": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]{1,9})?Z$",
      "description": "RFC 3339 UTC instant with Z offset. DESIGN.md does not fix a timestamp wire encoding; this derivation freezes with the bundle (gap note G3)."
    },
    "digestRef": {
      "type": "object",
      "additionalProperties": false,
      "required": ["class", "algorithm", "value_hex"],
      "$comment": "The closed family DigestRef wire (family-vectors/PROFILE.md §6.1, normative; D-R0-1): exactly the members {class, algorithm, key_ref?, value_hex}. The oneOf pins the closed class/algorithm pairing and requires key_ref exactly for the keyed erasure classes; each arm refines members of this already-closed object.",
      "properties": {
        "class": {
          "enum": [
            "structural_public",
            "portable_public",
            "local_erasure_safe",
            "scope_erasure_safe",
            "disclosed_party",
            "ciphertext_public"
          ],
          "description": "Digest class (PROFILE.md §6, D-R0-1); substituting one class where a schema requires another is a conformance failure."
        },
        "algorithm": { "enum": ["sha-256", "hmac-sha-256"] },
        "key_ref": {
          "type": "string",
          "pattern": "^[\\x21-\\x7e]{1,128}$",
          "description": "Key id for the keyed erasure classes; raw HMAC keys, per-object secrets, and salts are never part of a DigestRef."
        },
        "value_hex": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$",
          "description": "Exactly 64 lowercase hex characters."
        }
      },
      "oneOf": [
        {
          "properties": {
            "class": {
              "enum": [
                "structural_public",
                "portable_public",
                "disclosed_party",
                "ciphertext_public"
              ]
            },
            "algorithm": { "const": "sha-256" }
          },
          "not": { "required": ["key_ref"] }
        },
        {
          "properties": {
            "class": { "enum": ["local_erasure_safe", "scope_erasure_safe"] },
            "algorithm": { "const": "hmac-sha-256" }
          },
          "required": ["key_ref"]
        }
      ],
      "description": "Typed DigestRef: every digest field is typed, never an unlabelled hash. Wire shape, class/algorithm pairing, and key_ref presence per the ratified family profile."
    }
  }
}
