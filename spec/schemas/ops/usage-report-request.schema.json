{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://byom.example/bpp/v0/ops/usage-report-request.schema.json",
  "title": "BPP usage_report request (DESIGN.md \u00a711.4; registry R30 runtime)",
  "description": "Mutation (meta required, create-classed). Two arms, and the CHANNEL \u2014 not this flag \u2014 decides which is admissible: a `worker_report` is evidence and settles nothing (family contract L33: participant and worker reports are evidence, not meters), while `trusted_meter` requires the narrow meter adapter's workload token and carries the complete measured-settlement group. A worker report carrying any settlement member is refused by the closed shape.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "version",
    "op",
    "meta",
    "episode_ref",
    "generation",
    "byom_attempt_ref",
    "byom_fence_epoch",
    "kovee_invocation_fence",
    "source",
    "stable_report_key",
    "quantities"
  ],
  "properties": {
    "version": {
      "$ref": "#/$defs/protocolVersion"
    },
    "op": {
      "const": "usage_report"
    },
    "meta": {
      "$ref": "#/$defs/mutationMeta"
    },
    "episode_ref": {
      "$ref": "#/$defs/identifier"
    },
    "generation": {
      "$ref": "#/$defs/safeNonNegativeInteger"
    },
    "byom_attempt_ref": {
      "$ref": "#/$defs/identifier"
    },
    "byom_fence_epoch": {
      "$ref": "#/$defs/safeNonNegativeInteger"
    },
    "kovee_invocation_fence": {
      "$ref": "#/$defs/safeNonNegativeInteger"
    },
    "source": {
      "enum": [
        "worker_report",
        "trusted_meter"
      ]
    },
    "stable_report_key": {
      "$ref": "#/$defs/identifier"
    },
    "quantities": {
      "type": "array",
      "minItems": 1,
      "maxItems": 64,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "dimension",
          "unit",
          "amount"
        ],
        "properties": {
          "dimension": {
            "$ref": "#/$defs/identifier"
          },
          "unit": {
            "$ref": "#/$defs/identifier"
          },
          "amount": {
            "$ref": "#/$defs/safeNonNegativeInteger"
          }
        }
      },
      "description": "Reported or measured quantities."
    },
    "meter_ref": {
      "$ref": "#/$defs/identifier"
    },
    "meter_attestation_ref": {
      "$ref": "#/$defs/identifier"
    },
    "pricing_revision_ref": {
      "$ref": "#/$defs/identifier"
    },
    "stable_settlement_key": {
      "$ref": "#/$defs/identifier",
      "description": "The \u00a711.4 UsageSettlement head key: monotonic, stable-keyed, applied once on both sides."
    },
    "charged_quantities": {
      "type": "array",
      "minItems": 1,
      "maxItems": 64,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "dimension",
          "unit",
          "amount"
        ],
        "properties": {
          "dimension": {
            "$ref": "#/$defs/identifier"
          },
          "unit": {
            "$ref": "#/$defs/identifier"
          },
          "amount": {
            "$ref": "#/$defs/safeNonNegativeInteger"
          }
        }
      },
      "description": "The charged quantities; the charge never exceeds the reserved amount."
    }
  },
  "$defs": {
    "protocolVersion": {
      "type": "string",
      "pattern": "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)$",
      "description": "Negotiated protocol minor version, e.g. \"0.2\" (\u00a714.1)."
    },
    "identifier": {
      "type": "string",
      "pattern": "^[\\x21-\\x7e]{1,128}$",
      "description": "Opaque visible-ASCII identifier, at most 128 bytes (\u00a714.9)."
    },
    "safeNonNegativeInteger": {
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991,
      "description": "I-JSON safe integer (\u00a714.2 rejects unsafe integers)."
    },
    "timestamp": {
      "type": "string",
      "pattern": "^[0-9]{4}-(0[1-9]|1[0-2])-(0[1-9]|[12][0-9]|3[01])T([01][0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9](\\.[0-9]{1,9})?Z$",
      "description": "RFC 3339 UTC instant with Z offset, semantically valid in the proleptic Gregorian calendar: the pattern pins the lexical ranges and implementations MUST additionally reject impossible calendar instants (e.g. 2026-02-30) \u2014 the conformance runner enforces this on every vector; leap seconds are not accepted. DESIGN.md does not fix a timestamp wire encoding; this derivation freezes with the bundle (gap note G3, amended per RT-17)."
    },
    "digestRef": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "class",
        "algorithm",
        "value_hex"
      ],
      "$comment": "The closed family DigestRef wire (family-vectors/PROFILE.md \u00a76.1, normative; D-R0-1): exactly the members {class, algorithm, key_ref?, value_hex}. The oneOf pins the closed class/algorithm pairing and requires key_ref exactly for the keyed erasure classes; each arm refines members of this already-closed object.",
      "properties": {
        "class": {
          "enum": [
            "structural_public",
            "portable_public",
            "local_erasure_safe",
            "scope_erasure_safe",
            "disclosed_party",
            "ciphertext_public"
          ],
          "description": "Digest class (PROFILE.md \u00a76, D-R0-1); substituting one class where a schema requires another is a conformance failure."
        },
        "algorithm": {
          "enum": [
            "sha-256",
            "hmac-sha-256"
          ]
        },
        "key_ref": {
          "type": "string",
          "pattern": "^[\\x21-\\x7e]{1,128}$",
          "description": "Key id for the keyed erasure classes; raw HMAC keys, per-object secrets, and salts are never part of a DigestRef."
        },
        "value_hex": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$",
          "description": "Exactly 64 lowercase hex characters."
        }
      },
      "oneOf": [
        {
          "properties": {
            "class": {
              "enum": [
                "structural_public",
                "portable_public",
                "disclosed_party",
                "ciphertext_public"
              ]
            },
            "algorithm": {
              "const": "sha-256"
            }
          },
          "not": {
            "required": [
              "key_ref"
            ]
          }
        },
        {
          "properties": {
            "class": {
              "enum": [
                "local_erasure_safe",
                "scope_erasure_safe"
              ]
            },
            "algorithm": {
              "const": "hmac-sha-256"
            }
          },
          "required": [
            "key_ref"
          ]
        }
      ],
      "description": "Typed DigestRef: every digest field is typed, never an unlabelled hash. Wire shape, class/algorithm pairing, and key_ref presence per the ratified family profile."
    },
    "mutationMeta": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "request_id",
        "idempotency_key",
        "expected_endpoint_incarnation",
        "expected_recovery_epoch"
      ],
      "properties": {
        "request_id": {
          "$ref": "#/$defs/identifier"
        },
        "idempotency_key": {
          "$ref": "#/$defs/identifier"
        },
        "expected_endpoint_incarnation": {
          "$ref": "#/$defs/identifier"
        },
        "expected_recovery_epoch": {
          "$ref": "#/$defs/safeNonNegativeInteger"
        },
        "causation_event_ref": {
          "$ref": "#/$defs/identifier"
        },
        "correlation_ref": {
          "$ref": "#/$defs/identifier"
        }
      },
      "description": "MutationMeta (\u00a714.2), create-classed: this operation creates a record (or CASes a subordinate head through its own body fields), so the closed create meta carries NO expected_revision member \u2014 supplying one fails the closed schema (RT-01: update CAS is structural, never optional)."
    }
  },
  "oneOf": [
    {
      "properties": {
        "source": {
          "const": "worker_report"
        },
        "meter_ref": false,
        "meter_attestation_ref": false,
        "stable_settlement_key": false,
        "charged_quantities": false,
        "pricing_revision_ref": false
      },
      "$comment": "Participant and worker reports are EVIDENCE, not meters: this arm carries no settlement member at all (family contract L33)."
    },
    {
      "required": [
        "meter_ref",
        "meter_attestation_ref",
        "stable_settlement_key",
        "charged_quantities"
      ],
      "properties": {
        "source": {
          "const": "trusted_meter"
        }
      },
      "$comment": "Only the narrow trusted-meter adapter settles, and it carries the complete measured-settlement group (\u00a711.4)."
    }
  ]
}
