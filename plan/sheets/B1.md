# Milestone sheet — B1: A society of two

- **Owner:** byom
- **Prerequisites:** B0.1 (contract-ready), C3a (contract-ready — the attached
  channel it hosts)
- **Operation/schema bundle:** the B0.1 bundle, implemented in `byomd`
  (personal profile: SQLite, per-surface Unix sockets, developer security
  profile honestly labeled)
- **Scope (attached-only at I0 — the hosted/episodic slice moves to I1 with
  K2/C3b):**
  - one human (direct governance channel, fresh phishing-resistant challenge)
    + one agent participant with an `attached_harness` Manifestation
  - atomic `society_bootstrap` genesis; Charter r1; human sovereign seat;
    budget roots; event genesis
  - full onboarding state machine incl. refusal, revocation, expiry, and the
    candidate-channel fencing rules
  - participant-owned self-policies (`ParticipantAssentPolicyRevision`,
    `ActivationPolicyRevision` adoption paths); provenance-labeled assent modes
  - exploration `ActivityStream` before any pledge; directly authored
    `wake_intent_submit`
  - call → pledge → deterministic delivery → review; budgets reserved/settled;
    events cursored
  - kill-and-resume of the **attached** participant via portable
    `Continuation` (cross-Manifestation resume on a *hosted* runtime is I1)
- **Produced artifacts:** `byomd` + `byom` CLI (personal profile); numbered
  SQLite migrations; authority journal with the developer-recovery witness
  profile (honestly advertised as "developer recovery" — B-ADR-3 backend not
  required yet)
- **Assurance profile:** developer (labeled); no confinement claims
- **Verification (test IDs):**
  - `b1-acceptance`: agent notices/explores first, then accepts one Pledge,
    survives daemon kill/restart at every commit point, delivers a reviewable
    change-set with a complete causal timeline (byom §24 B1 exit)
  - `b1-onboarding-negative`: the C3a negative suite server-side — nothing but
    the candidate accepts; nothing but governance admits; terminal offers
    never admit; stale acceptance expires
  - `b1-journal`: authority-journal CAS before visibility; rollback of the DB
    under the journal produces `sealed_diagnostic`, never resurrected authority
  - `b1-vectors`: both independent clients replay the B0.1 vectors against the
    live daemon
- **Review evidence:** R1 (after I0) — L1, L2, L3
- **Size:** L
- **Integration gate relation:** feeds I0 (with K1 + C3a)
